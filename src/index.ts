/**
 * HTML Builder - Universal HTML generation library
 *
 * A zero-dependency TypeScript library for creating HTML elements
 * in both server-side rendering (SSR) and client-side environments.
 */

export * from "./htmlBuilder";
export { default } from "./htmlBuilder";
